# Core pipeline (features, anomaly, guideline retrieval, rules, coach, CLI)
# uses only the Python standard library on purpose, so `python -m health_coach.cli demo`
# works with zero installs. These are only needed to run `serve` (the local
# HTTP bridge for the Flutter app) and the test suite.
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
pytest>=8.3
